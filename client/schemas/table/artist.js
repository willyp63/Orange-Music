import { ARTIST_LINK_LOCATION } from './shared';

const SCHEMA = {
  titlePath: 'name',
  titleLabel: 'Name',
  titleLinkLocation: ARTIST_LINK_LOCATION,
  subtitlePath: '@NA',
  imagePath: 'image',
  actions: {},
};

export default SCHEMA;
